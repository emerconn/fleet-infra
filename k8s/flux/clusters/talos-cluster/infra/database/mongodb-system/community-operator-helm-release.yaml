apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mongodb-community-operator
  namespace: mongodb-system
spec:
  values:
    operator:
      watchNamespace: "unifi-network-application"
