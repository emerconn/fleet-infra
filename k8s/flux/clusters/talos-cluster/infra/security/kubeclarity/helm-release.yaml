apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeclarity
  namespace: kubeclarity
spec:
  values:
    kubeclarity:
      prometheus:
        enabled: false
        serviceMonitor:
          enabled: true
