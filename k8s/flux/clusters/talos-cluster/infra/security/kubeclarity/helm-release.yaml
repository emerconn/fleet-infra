apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeclarity
  namespace: kubeclarity
spec:
  values:
    kubeclarity:
      prometheus:
        enabled: true
        serviceMonitor:
          enabled: true
