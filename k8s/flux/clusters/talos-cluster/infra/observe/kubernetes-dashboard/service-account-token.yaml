apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: sa-token
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: kubernetes-dashboard
# to get the token:
# kubectl get secret -n headlamp service-account-token -o json | jq -r '.data.token' | base64 -d
