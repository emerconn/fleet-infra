apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mktxp
  namespace: mktxp
  labels:
    app: mktxp-exporter
spec:
  selector:
    matchLabels:
      app: mktxp-exporter
  endpoints:
  - port: metrics
    path: /
    interval: 30s
