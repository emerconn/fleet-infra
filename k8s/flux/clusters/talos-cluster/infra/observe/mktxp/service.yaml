apiVersion: v1
kind: Service
metadata:
  name: mktxp-exporter
  namespace: mktxp
  annotations:
    prometheus.io/port: "49090"
    prometheus.io/scrape: "true"
spec:
  selector:
    app: mktxp-exporter
  ports:
  - name: metrics
    port: 49090
    targetPort: 49090
